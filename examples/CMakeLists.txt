add_executable(ranklens_ping_pong ping_pong.cpp)
target_compile_features(ranklens_ping_pong PRIVATE cxx_std_17)
target_link_libraries(ranklens_ping_pong PRIVATE MPI::MPI_CXX)
add_executable(ranklens_nonblocking nonblocking.cpp)
target_link_libraries(ranklens_nonblocking PRIVATE MPI::MPI_CXX)
add_executable(ranklens_completion_families completion_families.cpp)
target_compile_features(ranklens_completion_families PRIVATE cxx_std_17)
target_link_libraries(ranklens_completion_families PRIVATE MPI::MPI_CXX)
add_executable(ranklens_persistent_requests persistent_requests.cpp)
target_compile_features(ranklens_persistent_requests PRIVATE cxx_std_17)
target_link_libraries(ranklens_persistent_requests PRIVATE MPI::MPI_CXX)
add_executable(ranklens_thread_multiple thread_multiple.cpp)
target_compile_features(ranklens_thread_multiple PRIVATE cxx_std_17)
target_link_libraries(ranklens_thread_multiple PRIVATE MPI::MPI_CXX)
add_executable(ranklens_controlled_errors controlled_errors.cpp)
target_compile_features(ranklens_controlled_errors PRIVATE cxx_std_17)
target_link_libraries(ranklens_controlled_errors PRIVATE MPI::MPI_CXX)
if(RANKLENS_HAVE_MPI_PARTITIONED)
  add_executable(ranklens_partitioned_requests partitioned_requests.cpp)
  target_compile_features(ranklens_partitioned_requests PRIVATE cxx_std_17)
  target_link_libraries(ranklens_partitioned_requests PRIVATE MPI::MPI_CXX)
endif()
